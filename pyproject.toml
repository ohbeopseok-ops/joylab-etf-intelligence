[project]
name = "joylab-etf-intelligence"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.0",
    "pydantic>=2.8",
]

[tool.vercel]
entrypoint = "api.telegram:handler"
