# V0.1.4.1

## 핵심 변경

1. paper / real 토큰 캐시 완전 분리
   - tokens/kis_paper_token.json
   - tokens/kis_real_token.json

2. HTTP 4xx/5xx 발생 시 traceback 속 URL 대신
   KIS 응답 body를 정리해서 출력

3. 잔고조회 오류 context에 계좌번호를 넣지 않음

## 실전 전환

.env:

KIS_ENV=real
KIS_APP_KEY=<실전키>
KIS_APP_SECRET=<실전시크릿>
KIS_ACCOUNT_NO=<앞8자리>
KIS_ACCOUNT_PRODUCT_CODE=<뒤2자리>

## 실행

기존 구버전 캐시는 그대로 둬도 V0.1.4.1이 읽지 않음.

실전 토큰 캐시 제거:
python scripts/clear_kis_token.py

잔고조회:
python scripts/smoke_account_balance_v141.py

회귀:
pytest tests/gold_cases -q
